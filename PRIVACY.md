# Geo Action 隐私政策

最后更新日期：2026 年 7 月 17 日

Geo Action 是一款 Chrome 浏览器扩展，用于把用户主动上传或输入的 Markdown 内容，按用户选择的平台规则适配并创建为可继续编辑的内容平台草稿。本扩展不会自动公开发布内容。

## 我们处理的数据

- 用户提供的标题、摘要、标签、Markdown 正文、封面、图片链接和本地图片。
- 目标平台必要的登录 Cookie、会话状态或 CSRF 状态。
- 平台登录状态、草稿 ID、编辑链接、任务阶段、错误和时间。
- 本地保存的工作区、平台选择、发布清单、设置、任务历史和重复指纹。
- Twitter/X 与小红书草稿流程中，为写入和验证草稿所需的目标编辑器状态。

我们不持续监控浏览历史，不扫描无关网页，不采集广告标识或行为分析数据，不把平台 Cookie、正文或图片发送给扩展开发者。

## 使用、存储与共享

数据仅用于解析和适配 Markdown、检查登录、转存用户选择的图片、创建和验证草稿、恢复本地状态及避免重复草稿。

本地状态保存在 `chrome.storage.local`，直至用户清除扩展数据或卸载扩展。平台 Cookie 由 Chrome 和目标平台管理，不复制到 Geo Action 记录。用户点击创建草稿后，必要内容只会直接发送到用户选择的平台；远程图片会从 Markdown 明确引用的来源读取并转存。

开发者不运营内容中转服务器，不接收用户草稿或平台 Cookie。我们不出售、出租或将数据用于广告、数据经纪、信用评估、贷款或与草稿创建无关的用途。

## 权限

- `storage`、`unlimitedStorage`：保存本地工作区、设置、历史以及较长 Markdown 和图片数据。
- `cookies`：检查目标平台登录状态并读取草稿流程必要的会话或 CSRF Cookie。
- `declarativeNetRequest`、`declarativeNetRequestWithHostAccess`：临时设置平台接口所需请求头，任务后清理。
- `sidePanel`：展示编辑与草稿工作流。
- `tabs`：打开登录页、原生编辑器、草稿和支持页面。
- `scripting`：仅在用户选择的 Twitter/X 或小红书编辑器中执行扩展包内脚本，写入、保存并验证草稿。
- `http://*/*`、`https://*/*`：访问平台注册端点和用户 Markdown 明确引用的远程图片。

## 安全与 Limited Use

所有可执行代码均包含在扩展包中，不加载远程代码。平台请求使用 HTTPS。身份验证 Cookie 不写入任务历史或开发者日志。遇到验证码、风控或安全验证时，扩展停止对应平台。

Geo Action 对从 Chrome 权限和目标平台获得的信息的使用，将遵守 Chrome Web Store User Data Policy（包括 Limited Use 要求）。数据仅用于本扩展明确披露的单一用途；除完成用户主动选择的草稿创建所必需外，不向第三方转移，不用于广告、数据经纪、信用评估，也不允许人工读取用户内容。

## 用户控制

用户可以编辑或清除本地状态、卸载扩展删除全部本地数据、不选择任何特定平台，并在各平台草稿箱查看、编辑或删除已创建草稿。

## 联系方式

- 支持与隐私问题：<https://github.com/WaterDJiang/geo-action-privacy/issues>
- 开发者网站：<https://www.wattter.cn>

